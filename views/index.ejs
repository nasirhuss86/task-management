
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

    <div class="main w-full  min-h-screen bg-zinc-900 ">
        <div class="form p-10 text-white ">
            <form action="/create" method="post">
                <input class="block px-3 py-2 w-full rounded-lg bg-zinc-800 outline-none" type="text" name="title" placeholder="text goes inside ">
                <textarea class="block mt-2 px-3 py-2 w-full rounded-lg bg-zinc-800 outline-none resize-none" name="details" placeholder="write your task details" id=""></textarea>
                <input class="mt-2 bg-blue-600 px-3 py-2 rounded-lg" type="submit" value="Create Task ">
            </form>
        </div>

        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if(files.length > 0 ){ %>
               <% files.forEach(function(val){ %>
                 <div class="task min-w-72  px-3 py-4 rounded-md bg-zinc-800">
                    <h1 class="text-white text-3xl tracking-tight "><%= val%></h1>
                    <a class="text-blue-500 inline-block mt-3" href="/file/<%= val %>">Read more.</a>
                 </div>
               <% }) %>
            <% } else{ %>
                <h3 class="text-zinc-600">no task found</h3>
            <% } %>             
        </div>
    </div>
</body>
</html>